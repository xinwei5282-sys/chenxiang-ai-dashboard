## 当前：快慢分层的模拟运行（2026-09-18）

用户明确选择接近真实使用的快慢分层增长。`src/simulation.mjs` 是独立模拟数据源，`src/simulation-view.mjs` 绑定显示：浏览量2–5秒、AI调用4–8秒、流通记录8–15秒、加工15–25秒、档案20–40秒、批次30–50秒不规则递增；基地8/12和链路6个固定。总档案始终等于三款商品档案合计，原料节点与顶部批次一致。数字650ms过渡；新业务记录写入滚动列表视区外，同步两份列表，不重启滚动。种子记录时间改为当前时间倒推，新记录用当前模拟到达时间。后台暂停增长，刷新重开模拟，减少动态效果时直接更新数字。未接真实接口。验收`npm test`、`npm run verify:simulation`、`npm run verify:alerts`。覆盖下方“固定时间、不新增记录”的历史约定。

## 当前：实时预警向上循环滚动（2026-09-18）

用户明确要求实时预警动态滚动并增加数据。`src/screen.mjs` 保留20条固定展示样例，涵盖种植、原料、加工、检测、仓储和流通；不是实时业务告警。392×216面板内保留5行可视区，通过两组相同列表和80秒线性动画无缝向上循环，约4秒一行。复制组用aria-hidden隐藏；系统减少动态效果时停动画。覆盖历史“无轮播”中对预警列表的限制，其余区域保持原样。聚焦验收：`npm run verify:alerts`。

## GitHub Pages 发布（2026-09-16）

用户已授权新建独立公开仓库 `xinwei5282-sys/chenxiang-ai-dashboard` 并开启网页预览。Pages 从 main 根目录部署，入口 `https://xinwei5282-sys.github.io/chenxiang-ai-dashboard/`。保持HTML/JS资源相对页面、CSS资源相对样式文件的路径，不恢复 `/assets/` 或 `/src/` 根路径。`.nojekyll` 用于直接托管静态文件。

## 当前：使用用户提供的招手 IP（2026-09-16）

数字人已替换为 `assets/selected/assistant-wave-source.png`。使用用户提供的透明PNG原像素，仅裁去透明留白；入口 `scripts/prepare_assistant_character.py`。保持342px高度并等比显示，left61px让身体落在光晕台面中间。旧三视图人物仅保留为历史素材；重跑助手提取脚本优先应用招手IP。

## 历史：使用用户提供的数字人三视图（2026-09-16）

右侧数字人使用 `assets/selected/assistant-character-reference.png` 的正面人物，保留原图RGB，通过 `scripts/extract_selected_character.py` 的轮廓与GrabCut提取透明素材；不再使用旧参考图中的人物。显示高度342px、宽度自动等比，沿用原光晕、台面及书法。重跑旧助手提取脚本会在最后应用新人物，避免恢复错误造型。

## 当前：标题底板避让中央树与 Git（2026-09-16）

标题底板按文字收窄，左右渐隐，使用透明蒙版避让中央树区域；标题位置、字号及文字显示保持原设定。大屏现独立使用本目录 Git 仓库，历史验收截图和备份目录 `audit/` 不入库；旁边 `prototype/` 是小程序独立仓库。以下无 Git 仓库的说明为历史记录。

## 最新微调：节点标题字号与底板透明度（2026-09-16）

六节点标题改为16px、600字重；说明保持14px。底板渐变alpha为0.60至0.55，更透明；保留既定坐标和覆盖ICON下缘的层级。

## 当前：标题原图位置、底板前置、原底弧上移（2026-09-16）

用户连续明确三项：标题位置参考图；标题背景置于ICON上层；底部连线使用上一版弧形直接上移，撤销斜向重画。当前六标签top分别为育苗384、基地324、原料389、加工541、产品665、流通541，横向中心与各自ICON一致。`.node-caption` z-index2，`.node-hit` z-index1；底板遮盖重叠圆圈下缘，取消挖空圆圈的背景蒙版，按用户最新要求覆盖旧避让规则。

连接层继续提取参考上弧；`scripts/extract_trace_connections.py` 将上一版下弧原像素整体上移63px（按最新反馈较上一版再下移20px），保留曲率、平行线和亮点，低点约650px，落在产品图标偏下位置，图标/文字遮罩在平移后应用。不再画新的斜向分支，也不把下弧留在标题下方。保留指定循环视频及ICON主体。

## 当前：参考图节点标题（2026-09-16）

用户要求图标下方标题也参考效果图。六组文字置于 `.node-caption` 居中两行标签：标题18px/650字重/25px行高，说明14px/21px行高，174px最小宽度及淡深蓝渐变底板。节点占位改为108px，标签从圆心下48px起，不覆盖最大47px圆环；覆盖之前120px占位规则。连线提取脚本的文字避让同步该区域。保持循环视频、六个图标与轨道位置。

## 当前：保留循环视频，按上传图对齐图标与轨道（2026-09-16）

用户明确选择“保留循环视频，只对齐六图标和轨道”，不得为了整景一致替换视频、树或基座。当前连接素材为 `assets/extracted/scene/trace-connections.png`，由 `scripts/extract_trace_connections.py` 从 `assets/selected/orbit-reference.png`（用户上传图）提取可见光轨与亮点；旧标签遮挡和下方建筑干扰处使用测量轨道补齐。拟合轨道中心(835,505.25)、半径(308.77,207.90)，下缘约713px；不得再压缩到637px或强制通过产品图标中心。六个图标保留当前原图PNG，data-layer4与视频独立。

同尺寸对照：`audit/orbit-reference-vs-current.png`。提取与补齐存在局部重建，不声称整张参考图无损还原。以下SVG与底部收紧方案均为历史版本。

## 当前：六节点环形连接（2026-09-16）

最新调整：轨道分为上方后弧与贴合基座的下方回弧，上弧保留内侧细线和局部光带，下弧降低亮度并收至产品圆圈后方；标签避让使用4px羽化遮罩，取消硬切断口。六个图标素材不变。轨道调整前备份：`audit/pre-orbit-depth-20260916/`。

根据用户提供的 `Codex 图像 2026年9月16日 11_10_08.png` 补齐六个ICON之间的蓝色环形轨道。新增 `assets/extracted/scene/trace-connections.svg`，包含主光线、内侧细线及蓝白亮点，data-layer4独立显示，位于六圆圈下方；文字区域仅遮罩连线，不加深色底板。轨道与六圆圈相交，随1671×941画布缩放。分层页新增4号连线控制；视频仍正常循环。

## 当前：按原始参考图恢复气泡素材（2026-09-16）

用户再次要求根据参考图处理，停止CSS拟制圆圈。当前六张trace PNG直接保留 `assets/selected/reference.png` 中各自主体、可见圆环、明暗变化和亮点；只对文字遮挡的下部月牙区域，用源图上弧镜像及种植基地清洁内底颜色补齐，并保护主体轮廓。外侧透明羽化。`src/layers.css`禁用气泡伪元素，避免双圈。不可恢复颜色键抠主体或CSS均匀描边方案。以本条覆盖以下气泡历史方案。备份：`audit/pre-reference-bubbles-20260916/`。

## 当前气泡配色：恢复原科技蓝（2026-09-16）

用户指出透明玻璃颜色不搭，要求参考之前效果。保持主体/圆圈分离，气泡改为原风格的深蓝内底、亮蓝圆环、蓝色内外发光及上缘光线。取消灰白玻璃高光与背景模糊；六圈统一、径向渐变连续，不恢复底部图片拼接分界。此条覆盖下文玻璃配色。

## 当前：六图标玻璃气泡（2026-09-16）

用户要求优化气泡效果。六张trace PNG现在只含透明主体，通过轮廓与颜色蒙版提取，移除原圆圈；`src/layers.css` 的 `.node-hit::before/::after` 统一绘制94px玻璃气泡、连续透明渐变、柔和细边及左上高光。保持节点位置、120px占位与下方标签。无新增动画或交互。分层工具的3号图层同时控制气泡与主体。此条覆盖此前复用原图圆环规则，防止恢复带水平透明分界的圆圈。备份：`audit/pre-glass-bubbles-20260916/`。

## 当前：统一复用种植基地圆圈（2026-09-16）

用户明确要求其余五个直接使用种植基地的圈。`complete_trace` 保持种植基地PNG原样；其余五张共用该圆环及完整下缘，内侧去除树主体后按源图轮廓贴入各自图标，不再保留其他图标原圆圈。外环半径42px以外及底部90行起RGBA与种植基地逐像素一致。以本条覆盖此前五圈局部清黑方案；生成入口仍为 `scripts/extract_icons.py --trace-only`。

## 最新：六个溯源圆圈下部透明（2026-09-16）

用户确认种植基地圆圈正常。保持该素材不变，其余五张继续清除原图下部残留的深色底及投影；按颜色保留主体与发光圆环。只清理被标签遮挡的小段仍会留下黑色横带，不能退回该方案。

用户要求去掉中间六个圆形图标下半截黑色。`scripts/extract_icons.py` 的下缘修复不再填充不透明深蓝色，改为透明内部与独立发光圆环；主体上部原始像素保留。`--trace-only` 仅更新六张trace图标和对应元数据，不动AI图标。不可恢复旧深色补底。编辑前备份在 `audit/pre-icon-transparency-20260916/`。 同时 `.trace-node .node-hit` 占位高度必须匹配120px图标，标签排在完整圆圈下方；不可恢复91–101px占位、产品文字深色底板或12px文字重阴影，以免再次遮住下半圈。

## 最新：循环视频背景（2026-09-16）

用户指定桌面 `山谷光球_粒子增强版_v2_1080p_副本.mp4` 替换大屏背景。当前底层为 `assets/video/valley-sphere-particles-v2-1080p.mp4`（1920×1080、8秒、无音轨），默认 autoplay / muted / loop / playsinline，不显示播放控件，不响应点击；首帧海报负责加载占位。保留1671×941画布等比适配、所有文字、指标、面板和助手层。视频无六节点图标，因此恢复现有6个独立图标，与4个AI能力图标同属data-layer3；不重复叠加旧背景和中心树。服务端支持video/mp4及Range请求，便于播放和循环。以下静态背景规则如有冲突以本条为准。

## 最新：顶部光带与光翼完整范围（2026-09-11）

标题背景改为全宽1671×97，left0；不再局限797px标题区域。横向光带保留左右延伸，光翼提取范围覆盖全部平行线及外围发光，不再使用窄线裁切。src/layers.css 与 screen.mjs 背景尺寸同步。素材自身不带山林RGB，中央仍是干净深蓝半透明玻璃。

## 最新：标题背景按原图重切，不保留山景（2026-09-11）

header-background.png 为797×97，坐标437,0。scripts/extract_header.py 仅提取原图两侧光翼、斜线和下缘光带，中间为纯色深蓝半透明玻璃，外侧透明，不含山林或文字。extract_chrome.py 调用独立脚本保持结果。不得恢复山景纹理。文字继续由HTML显示。

## 最新：大屏纯展示，移除点击效果（2026-09-11）

首页所有指标、能力、预警、节点、产品、助手、状态条与底部内容均为静态div，不再渲染button/data-open/dialog，不再绑定点击处理。去除悬停增亮与节点焦点光圈，页内全屏按钮同步移除。保留时钟更新、缩放适配；layers.html及cutouts.html工具页正常使用。测试检查各区点击不弹窗、悬停样式不变。

## 最新小改：Logo宽190px，删除三个面板标题旁的更多入口。保持Logo等比与原水平中心位置。

## 最新：从原图重新提取助手几何光晕（2026-09-11）

scripts/restore_assistant_scene.py 已改为从 reference.png 测量可见蓝色弧线的轮廓与亮度，剔除人物遮挡样本及树纹，沿弧补全后输出干净透明蒙版；上方几何光面按可见形状局部重建，不再使用补背景的RGB。assistant-background.png 为相同的无台面光晕；assistant-scene.png 加回 assets/selected/assistant-platform-source.png（329行起）原台面。不可退回旧蓝底去色链。人物、题字不变，素材仍32张。

## 最新：助手背景透出全屏山景（2026-09-11）

assistant-scene.png 现为 RGBA：移除深蓝不透明底色，透出大屏山林；保留半透明科技光效、几何光效及原始脚下台面。scripts/restore_assistant_scene.py 通过去底色蒙版输出，不得退回纯光晕替换，也不得恢复不透明底色。人物、题字不变。中央原补背景留下的灰白雾块已在生成脚本中局部清除，保留上方几何光效和细弧线。

## 最新覆盖：助手只移除山林，保留其余场景（2026-09-11）

用户指出透明光晕替换删减过多。assistant-scene.png 已从 assets/selected/assistant-scene-source.png 恢复原场景科技光效和脚下台面，只把山林区域局部替换为深蓝氛围底色。人物、题字仍独立且不改；assistant-background.png 仍仅供单独光晕编辑，不得拿它覆盖主场景。scripts/restore_assistant_scene.py 固化此处理，底部8行原场景台面像素保持一致。原图为合成图，局部遮挡光效经过提取/补全，不声称拿到了原设计图层。

## 当前覆盖：Logo、透明助手背景及白块修复（2026-09-11）

- 用户要求删除“演示样例”标签，已移除主页角标及弹层同名文案；保留未接实时系统的事实说明。
- 左上角使用用户提供的金色透明 Logo，assets/selected/brand-logo.png，仅裁去透明留白。
- assistant-scene.png 已按最新要求移除山林，复用现有透明光晕；restore_assistant_scene.py 保持此输出，不得恢复旧山林场景。人物、书法不变。透明处透出全屏底图。
- full-scene.png 在 (1158,184) 至 (1465,550) 范围内以相邻山林纹理修复偏白拼接块；源图中央树及图标受保护。

## 最新：按原图恢复主体、助手与4个指标图标

full-scene.png不再拼接生成树，而从reference.png保留原中心树/球/平台/轨道与6图标像素，仅移除节点文字。外围被UI遮挡处仍使用补全山林并羽化过渡，不得声称全背景无损。生成scripts/compose_full_scene.py。
助手主页面改用assistant-scene.png恢复原山林与光晕背景；单独透明assistant-background.png作为编辑切片保留，主页面不重复叠加它。人物和书法仍独立。重建脚本extract_assistant.py结尾调用restore_assistant_scene.py。
顶部4指标图标必须使用metrics/archive,batch,nodes,ai.png，均80×80源RGB切图，data-layer11。不可恢复通用SVG或CSS重复圆框。4个指标框坐标同步原图。素材总数32。

## 最新：背景、中心树和六图标合并

按用户要求，主页面只加载assets/extracted/scene/full-scene.png（1671×941）作为data-layer1，包含底图、树/球/基座/轨道和6个溯源图标。不再单独叠加树图和6张icon；节点点击区域及原生标题/数量保留。data-layer3仅4个AI能力图标；4号图层不再使用。
六张溯源icon已修正为120×120透明留边画布，保留完整圆环；原图被标签覆盖的底部小段圆环局部补齐。原独立素材保留供编辑。合成脚本scripts/compose_full_scene.py；先运行extract_icons.py再合成。素材预览共27张（26分层素材+1合成场景）。

## 最新局部修订：光晕比例及顶部题字

助手光环外径由336×348缩至288×310，中心由191,201移至172,192，减淡右侧和底部弧线。画布仍398×342。右上角标语使用原图抠出的chrome/slogan.png，222×44，源位置1421,19，保留手写字形和下划线；data-layer10。素材总数26。不得改回系统字体模拟。

## 助手素材最新修订

用户要求assistant-background去除山林，只保留透明光晕；右侧书法及印章单独切图。现在共25张PNG，全部含alpha。data-layer7光晕、8人物、9书法。光晕398×342；书法64×256，相对偏移309,39。生成脚本scripts/extract_assistant_decor.py；重跑extract_assistant.py会调用它保留最新拆分。以下旧版背景描述不再适用。

# 最新：独立PNG切图（2026-09-11）

- 用户已明确同意本地像素裁切、蒙版抠图和局部背景补全。以参考合成PNG为准，禁止再次生成不同造型的数字人。
- `assets/extracted/` 包含24张独立PNG：9个面板、10个图标、状态条及顶底3个背景、数字人及背景2张。
- `src/layer-assets.mjs` 使用独立PNG，不再引用图集；框与顶底状态装饰使用PNG，不再用CSS模拟。
- data-layer 1–6延续原分组；7为助手背景，8为透明数字人。`layers.html` 可独显；`cutouts.html` 提供24张预览和单张下载。
- 原始PNG为合成图。图标、数字人保留原RGB；透明蒙版为测绘结果；玻璃内部、文字移除区、人物遮挡背景为局部重建。不得称为无损恢复设计源图层。
- 底图和中心组合沿用此前重建素材，仍与原图纹理/树形不同。本轮仅修改独立切图相关内容。
- 验证：`npm run check`、`node tests/selected-browser.mjs`；详情见`design-qa.md`。

以下为历史记录，冲突以上述最新约定为准。

# 广垦沉香固定展示大屏

## 六类素材分层（2026-09-11，最新用户指令）

以选定参考稿为准，严格保留六组独立视觉层：1全屏底图；2半透明框；3六溯源icon和四能力icon；4树/光球/基座/轨道组合；5状态条背景；6顶部及底部背景。

- 层结构和样式在 `src/layers.css`；图标原稿区域在 `src/layer-assets.mjs`。图层用data-layer="1"至"6"标识。
- 背景与中心组合图为独立图片，中心图含透明alpha、不含文字/节点。10个图标共用原稿图集，每个为独立元素。框、状态条、顶底装饰使用独立CSS元素。
- 所有节点名称、数量和状态都必须是可见前端文字。禁止恢复 `.reference-trace-art` 整块含字截图或用opacity:0隐藏节点文字。
- 只保持前端演示，不接真实业务/AI。右侧数字人区域本轮不拆分，仍复用原稿局部插画。
- 分层检查页 `/layers.html` 可单独显示每一层并恢复完整页；完整大屏入口仍 `/`，当前服务4187。
- 新证据 `audit/layers-icons.png`、`audit/layers-tree.png`，大屏截图与测试报告沿用selected文件。

## 客户选定方案复刻（首版记录，被上述分层约定覆盖）

- 用户明确先复刻前端页面。参考 `assets/selected/reference.png`，忠实还原深蓝山林、中央树球六节点、四项顶部指标、左侧能力/预警/品牌Banner、右侧数字人/产品TOP3、底部四入口。
- 当前入口仍为 `index.html` → `src/screen.mjs` / `src/screen.css`。参考坐标画布1671×941，整体等比适配1920×1080、1440×900、4K，不裁切、无展示滚动条。
- 仅前端样例，不接后端、真实AI或实时监测。展示数据与选定图一致；角落“演示样例”和各弹层保留样例说明。六节点的实时状态和风险结论只是参考图文案。
- 底部入口和节点只打开说明弹层，不跳第二张展示页。数字人仅有预设知识示例；品牌故事只显示文字介绍，未接入视频。
- 当前推荐预览 `http://127.0.0.1:4187/`，运行 `PORT=4187 npm run dev`。本目录无Git仓库，前一版备份在 `audit/pre-selected-20260911/`。
- 中央溯源与数字人使用参考原图区域作为插画，中央图内标签是图片内容；六节点用原生按钮提供可访问名称与点击反馈。其它标题/指标/列表/底部入口为HTML。外层森林与产品图为参考生成素材。
- 当前界面验收 `npm run verify:ui`，证据 `audit/selected-*.png`、`audit/selected-browser-report.json`，视觉对照记录 `design-qa.md`。
- 原有data.mjs/knowledge.mjs及其测试保留，但新版首页用选定图的静态样例，不声称它们是本屏指标来源。旧设置、筛选和导出不属于本轮前端复刻。

## 前一版记录（以下冲突以当前客户选定图为准）


## 单屏展示决策（2026-09-09）

- 用户明确要求只有一屏，所有展示内容同时可见；不使用页面导航、分页、模块标签切换、图表指标切换或轮播。
- 正式原型入口为 `index.html` → `src/screen.mjs` / `src/screen.css`，以1920×1080设计画布整体等比适配显示设备，不裁切，不出现展示滚动条。
- 对外介绍优先：顶部8项核心指标；中央全链路溯源贯穿两层，占主体约一半宽度；左侧知识建设/产品档案，右侧内容传播/智能问答。保持五面板同屏，减少解释文字，不恢复维护流水、审核状态及密集明细表。
- 溯源用六节点展示：产地来源→原料批次→加工工艺→检测证书→芯片绑定→数字生命卡。当前为流程示意，不能虚构实际溯源记录、区块链、实时物联网或鉴真承诺。
- 知识四组分布统计已发布条目，合计对应顶部已发布知识；维护量和新增量继续保留。
- 时间、产品、内容、演示角色及汇总导出放入设置弹层；指标说明与核对允许操作弹层，不能成为第二张展示页。
- 数据继续来自 `data.mjs` 和 `knowledge.mjs` 的构造样例。真实数据、权限鉴权未接入，必须保持样例标注。
- `app.mjs`、`style.css`、`knowledge.css` 是前一版多页实现，保留供追溯但不被入口加载；不恢复旧结构。
- 验收应检查整张画布的边界及面板内部内容，不能仅通过body无滚动条判断适配成功。主规格1920×1080，兼顾1440×900和4K等比适配。
